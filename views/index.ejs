<p>
    <a href="/feeds/new" title="nouveau feed">Créer un feed</a>
</p>

<h2>Feeds existants</h2>
<% if (object_list.length) { %>
<ul>
    <% object_list.forEach(function(feed) { %>
    <li>
        <%= feed.name %>
        <a href="/feeds/<%= feed.id %>/edit" title="éditer">Éditer</a>
        <a href="/feeds/<%= feed.id %>/delete" title="supprimer">Effacer</a>
    </li>
    <% }) %>
</ul>
<% } else { %>
    <p>Aucun feed a l'heure actuelle.</p>
<% } %>
